/**
 * 
 */

var Biegler = Biegler || {};

Biegler.Layer = class {

	constructor() {
		
	}
	
	onEvent(e) {
		Biegler.err("Override me.");
	}

	onUpdate() {
		Biegler.err("Override me.");
	}

	onAttach() {
		Biegler.err("Override me.");
	}

	onDetach() {
		Biegler.err("Override me.");
	}

}
 